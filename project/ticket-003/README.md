# Ticket 003: Negative AuthN fixtures (LC-011)

## Acceptance

- [x] Unknown profile fixture stays outside closed set.
- [x] `payment-as-membership` binding receipt denied as `AUTHN-PAY-001`.
- [x] `python3 standard/conformance.py --all` passes.
